# scenes/

Scènes Godot minimales : assemblage, collision et script attaché. La logique vit dans `scripts/`.

Une modification de chemin de script doit préserver le `.uid` associé.
Ne lancer `./verifier.sh` que sur demande explicite, conformément au processus léger de l'`AGENTS.md` racine.
